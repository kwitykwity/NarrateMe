# Base URL of the NarrateMe backend API. Defaults to http://localhost:8000 if unset.
# Copy this file to .env.local for local dev, or set it in your host (e.g. Vercel).
# NOTE: NEXT_PUBLIC_ vars are inlined at build time, so set it before/at build.
NEXT_PUBLIC_API_URL=http://localhost:8000
